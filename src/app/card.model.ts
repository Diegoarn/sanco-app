export interface Proyecto {
    id: number,
    imagen: string,
    nombre: string,
};

export interface DetalleProyectos {
    
        id: number,
        nombre: string,
        texto: string
    
}